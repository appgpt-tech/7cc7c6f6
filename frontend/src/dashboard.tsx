import { Card, CardContent, CardHeader } from "@mui/material";
    export const Dashboard = () => (
      <Card>
      <CardHeader title= "john" />
      <CardContent> John is an ecommerce webapp with multiple product categories, like tech, toys, cars. < /CardContent>
      </Card>
    );