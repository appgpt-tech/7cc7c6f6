import { Card, CardContent, CardHeader } from "@mui/material";
    export const Dashboard = () => (
      <Card>
      <CardHeader title= "apoelsite" />
      <CardContent> A fan app for the sports club APOEL FC, featuring entities such as Players, Matches, and News < /CardContent>
      </Card>
    );